# 球球大作战 - 游戏策划文档

## 📋 文档信息

| 项目 | 内容 |
|------|------|
| 游戏名称 | 球球大作战 (Battle of Balls) |
| 游戏类型 | 动作、策略、多人对战 |
| 技术栈 | Vue 3 + HTML5 Canvas |
| 文档版本 | v1.0 |
| 创建日期 | 2026-02-09 |

---

## 🎮 游戏概述

### 游戏简介
《球球大作战》是一款以球球为核心的多人在线竞技游戏。玩家控制自己的球球在游戏地图中吞噬小彩豆和其他玩家，不断成长壮大，同时躲避大球的追捕。游戏采用简单易懂的操作方式，但需要策略和技巧才能成为霸主。

### 核心卖点
- **简单易上手**：单手即可操作，适合碎片化时间
- **策略性强**：需要合理运用分身、吐球等技巧
- **竞技刺激**：实时对战，紧张刺激
- **成长满足感**：从小球成长为地图霸主的成就感

### 目标用户
- 喜欢休闲竞技游戏的玩家
- 碎片化时间较多的上班族/学生
- 喜欢策略思考的玩家

---

## 🎯 核心玩法

### 基础机制

#### 1. 球球成长
- **吞噬彩豆**：地图上随机分布大量彩色豆子，吞噬后增加质量
- **吞噬玩家**：可以吞噬比自己小的其他球球
- **被吞噬**：被比自己大的球球吞噬后游戏结束
- **质量衰减**：过大的球球会随时间缓慢衰减质量

#### 2. 移动机制
- **跟随鼠标**：球球跟随鼠标指针移动
- **质量影响速度**：球球越大，移动速度越慢
- **边界限制**：地图有边界，球球不能移出地图

#### 3. 核心技能

| 技能名称 | 操作方式 | 效果 | 冷却时间 |
|---------|---------|------|---------|
| **分身** | 鼠标左键/空格 | 将自身分裂成两半，向前方弹出 | 1.5秒 |
| **吐球** | 鼠标右键/W键 | 吐出自身质量的25%作为小孢子 | 0.5秒 |
| **合成** | 靠近分身 | 分身靠近时会自动合并 | 无 |

### 游戏模式

#### 模式一：单人挑战模式（推荐优先实现）
- **玩法**：玩家对抗AI控制的球球
- **难度**：提供简单/普通/困难三种难度
- **目标**：成为地图上最大的球球或存活一定时间
- **特点**：无需网络，可随时游戏

#### 模式二：无尽生存模式
- **玩法**：在有限地图中生存，躲避不断生成的AI敌人
- **目标**：尽可能长时间存活
- **特色**：地图边界会随时间缩小

#### 模式三：限时积分模式
- **玩法**：固定时间内尽可能吞噬更多
- **目标**：获得最高积分
- **特色**：各种道具出现（加速、护盾、双倍积分等）

---

## 🛠️ 技术实现方案

### 整体架构

```
球球大作战/
├── BallGame.vue              # 主游戏组件
├── canvas/
│   ├── GameCanvas.js        # Canvas渲染核心
│   ├── Ball.js              # 球球实体类
│   ├── Food.js              # 食物实体类
│   ├── Player.js            # 玩家控制类
│   └── AI.js                # AI逻辑类
├── utils/
│   ├── collision.js         # 碰撞检测
│   ├── quadtree.js          # 四叉树优化（可选）
│   └── gameUtils.js         # 游戏工具函数
└── assets/
    ├── bg.png               # 背景图
    └── sounds/              # 音效文件
```

### 核心技术点

#### 1. Canvas 渲染
```javascript
// 使用 requestAnimationFrame 实现流畅动画
class GameCanvas {
  constructor() {
    this.canvas = document.createElement('canvas')
    this.ctx = this.canvas.getContext('2d')
    this.entities = []
  }

  render() {
    this.ctx.clearRect(0, 0, this.width, this.height)
    this.entities.forEach(entity => entity.draw(this.ctx))
    requestAnimationFrame(() => this.render())
  }
}
```

#### 2. 碰撞检测
```javascript
// 圆形碰撞检测
function checkCollision(ball1, ball2) {
  const dx = ball1.x - ball2.x
  const dy = ball1.y - ball2.y
  const distance = Math.sqrt(dx * dx + dy * dy)
  return distance < ball1.radius + ball2.radius
}
```

#### 3. AI 简单逻辑
```javascript
class AI {
  update(target, nearbyBalls) {
    // 寻找最近的食物
    // 躲避比自己大的球
    // 追逐比自己小的球
    // 随机游走
  }
}
```

#### 4. 性能优化
- **对象池**：重用球球和食物对象，减少GC
- **视锥剔除**：只渲染视野内的实体
- **四叉树**：优化碰撞检测性能（可选）
- **LOD**：远距离球球简化渲染

### 与项目集成

#### 1. 路由配置
```javascript
// src/router/index.js
{
  path: '/ballbattle',
  name: 'BallBattle',
  component: () => import('../views/BallBattleView.vue'),
  meta: {
    title: '球球大作战',
    icon: '⚽'
  }
}
```

#### 2. 游戏列表配置
```javascript
// src/GameList/index.vue
{
  id: 'ballbattle',
  name: '球球大作战',
  englishName: 'Ball Battle',
  description: '吞噬彩豆和对手，成为地图上的霸主',
  icon: '⚽',
  path: '/ballbattle',
  color: '#FF6B6B',
  category: 'action', // 动作类
  tags: ['动作', '策略', '竞技', '多人'],
  difficulty: '中等',
  players: '单人/AI对战',
  isVueComponent: true
}
```

#### 3. 存档系统
```javascript
// 保存内容
{
  highScore: 15000,        // 最高分
  totalGames: 10,          // 总游戏次数
  totalKills: 25,          // 总击杀数
  bestRank: 1,             // 最佳排名
  unlockedSkins: [...]     // 已解锁皮肤
}
```

---

## 🎨 界面设计

### 主界面布局

```
┌──────────────────────────────────────┐
│           【球球大作战】              │
│                                      │
│  ┌──────────┐  ┌────────────────┐   │
│  │ 游戏模式 │  │   难度选择     │   │
│  │ 单人挑战 │  │   ○ 简单      │   │
│  │ 无尽生存 │  │   ● 普通      │   │
│  │ 限时积分 │  │   ○ 困难      │   │
│  └──────────┘  └────────────────┘   │
│                                      │
│        ┌──────────────┐             │
│        │  开始游戏    │             │
│        └──────────────┘             │
│                                      │
│  最高分: 15000   游戏次数: 10       │
└──────────────────────────────────────┘
```

### 游戏内界面

```
┌──────────────────────────────────────┐
│  排名: [3/20]    质量: 2500    🏆   │ ← 顶部HUD
│                                      │
│  ┌────────────────────────────┐     │
│  │                            │     │
│  │      Canvas 游戏区域        │     │
│  │     (地图、球球、食物)      │     │
│  │                            │     │
│  └────────────────────────────┘     │
│                                      │
│  [W:吐球] [空格:分身] [ESC:暂停]    │ ← 底部提示
└──────────────────────────────────────┘
```

### 暂停/结算界面

```
┌────────────────────────────┐
│       ⏸️ 游戏暂停          │
├────────────────────────────┤
│  当前质量: 2500            │
│  当前排名: 3/20            │
│  击杀数: 5                 │
├────────────────────────────┤
│  [继续游戏] [重新开始]     │
│  [返回主菜单]              │
└────────────────────────────┘
```

---

## 📊 功能模块详细设计

### 模块1: 球球实体 (Ball.js)

```javascript
class Ball {
  constructor(x, y, mass, color) {
    this.x = x              // X坐标
    this.y = y              // Y坐标
    this.mass = mass        // 质量
    this.color = color      // 颜色
    this.velocity = { x: 0, y: 0 }  // 速度向量
    this.name = ''          // 玩家名称
    this.skin = null        // 皮肤
  }

  // 根据质量计算半径
  get radius() {
    return Math.sqrt(this.mass) * 2
  }

  // 移动
  move(targetX, targetY) {
    // 计算移动方向
    // 质量越大速度越慢
  }

  // 分身
  split() {
    // 创建一个新球，质量减半
  }

  // 吐球
  eject() {
    // 创建一个小孢子向前飞出
  }
}
```

### 模块2: AI系统 (AI.js)

#### AI行为树
```
AI决策
├── 检测周围环境
│   ├── 找到最近的食物 → 移动去吃
│   ├── 检测到比自己大的球 → 躲避
│   └── 检测到比自己小的球 → 追逐
├── 战术决策
│   ├── 自己很大 → 考虑分身捕猎
│   ├── 自己很小 → 保持隐蔽
│   └── 被追捕 → 使用分身逃脱
└── 边界处理
    └── 靠近边界 → 转向
```

#### AI难度调整
- **简单**：反应慢、经常犯错、不主动追击
- **普通**：正常反应、基本策略
- **困难**：反应快、会配合、具有威胁性

### 模块3: 地图系统

```javascript
class GameMap {
  constructor(width, height) {
    this.width = width
    this.height = height
    this.foods = []          // 食物数组
    this.virus = []          // 病毒（刺球）
    this.gridSize = 50       // 网格大小
  }

  // 生成食物
  spawnFood(count) {
    // 随机生成彩豆
  }

  // 生成病毒
  spawnVirus() {
    // 生成绿色的刺球
  }

  // 边界检测
  isInBounds(x, y) {
    return x >= 0 && x <= this.width && y >= 0 && y <= this.height
  }
}
```

### 模块4: 相机系统

```javascript
class Camera {
  constructor() {
    this.x = 0
    this.y = 0
    this.zoom = 1
  }

  // 跟随玩家
  follow(player) {
    // 相机中心对准玩家
    // 根据玩家大小调整缩放
  }

  // 世界坐标转屏幕坐标
  worldToScreen(x, y) {
    return {
      x: (x - this.x) * this.zoom + canvas.width / 2,
      y: (y - this.y) * this.zoom + canvas.height / 2
    }
  }
}
```

### 模块5: UI系统

#### 1. 排行榜
- 实时显示前10名
- 自己的排名高亮
- 按质量排序

#### 2. 小地图
- 显示玩家位置
- 显示周围敌人
- 显示边界

#### 3. 数据统计
- 当前质量
- 击杀数
- 存活时间
- 排名变化

---

## 🎯 皮肤系统

### 皮肤类型

| 皮肤 | 解锁条件 | 特点 |
|------|---------|------|
| 默认球 | 初始 | 纯色球体 |
| 星空球 | 游戏次数达到10次 | 星空纹理 |
| 彩虹球 | 击杀数达到50 | 渐变色彩 |
| 金属球 | 最高分达到10000 | 金属质感 |
| 幽灵球 | 存活时间达到30分钟 | 半透明效果 |

### 皮肤实现
```javascript
class Ball {
  draw(ctx) {
    ctx.save()
    ctx.beginPath()

    // 应用皮肤效果
    if (this.skin === 'rainbow') {
      const gradient = ctx.createRadialGradient(...)
      ctx.fillStyle = gradient
    } else if (this.skin === 'metal') {
      // 金属质感渲染
    }

    ctx.arc(this.x, this.y, this.radius, 0, Math.PI * 2)
    ctx.fill()
    ctx.restore()
  }
}
```

---

## 📈 数据统计与成就系统

### 成就列表

| 成就 | 条件 | 奖励 |
|------|------|------|
| 🎯 初出茅庐 | 完成第一局游戏 | 默认皮肤 |
| 💪 百斩杀手 | 累计击杀100个球 | 彩虹球皮肤 |
| 👑 称霸一时 | 成为地图第一 | 金属球皮肤 |
| 🏃 长跑健将 | 单局存活30分钟 | 幽灵球皮肤 |
| ⚡ 闪电战 | 5分钟内达到10000质量 | 特殊称号 |

### 数据埋点

```javascript
// 游戏开始
analyticsTracker.trackUserAction('ballbattle_game_start', {
  mode: 'single',
  difficulty: 'normal'
})

// 游戏结束
analyticsTracker.trackUserAction('ballbattle_game_end', {
  duration: 180,
  score: 15000,
  kills: 5,
  rank: 3
})
```

---

## 🚀 开发计划

### 第一阶段：核心功能（1-2周）
- [ ] Canvas 渲染框架搭建
- [ ] 球球实体类实现
- [ ] 基础移动和碰撞检测
- [ ] 食物系统
- [ ] 玩家控制

### 第二阶段：游戏机制（1-2周）
- [ ] 分身机制
- [ ] 吐球机制
- [ ] AI基础逻辑
- [ ] 地图边界
- [ ] 排行榜

### 第三阶段：优化完善（1周）
- [ ] 性能优化
- [ ] UI美化
- [ ] 音效添加
- [ ] 皮肤系统
- [ ] 存档系统

### 第四阶段：测试上线（3-5天）
- [ ] Bug修复
- [ ] 平衡性调整
- [ ] 与项目集成
- [ ] uTools插件配置

---

## ⚙️ 配置参数

### 游戏平衡参数

```javascript
const GAME_CONFIG = {
  // 地图设置
  mapWidth: 3000,
  mapHeight: 3000,
  gridSize: 50,

  // 球球设置
  initialMass: 50,
  minMass: 10,
  maxMass: 20000,

  // 速度设置
  baseSpeed: 5,
  massSpeedFactor: 0.05,

  // 食物设置
  foodCount: 500,
  foodMass: 1,
  foodSpawnRate: 0.1,

  // 分身设置
  splitCooldown: 1500,
  minSplitMass: 35,

  // 吐球设置
  ejectCooldown: 500,
  ejectMassLoss: 0.25,
  ejectMass: 15,

  // AI设置
  aiCount: 20,
  aiDifficulty: 'normal',

  // 游戏时长
  gameDuration: 300, // 秒
}
```

---

## 📝 参考资料

- [球球大作战WIKI - 基本规则](https://wiki.biligame.com/bob/%E5%9F%BA%E6%9C%AC%E8%A7%84%E5%88%99)
- [球球大作战WIKI - 入坑指南](https://wiki.biligame.com/bob/%E5%85%A5%E5%9D%91%E6%8C%87%E5%8D%97)
- Agar.io 源码参考（类似游戏）

---

## 📌 注意事项

1. **性能优先**：保证游戏在低端设备上也能流畅运行（60fps）
2. **操作简单**：确保新手玩家能快速上手
3. **平衡性**：AI难度需要反复调整，避免太简单或太难
4. **存档安全**：定期自动保存，防止意外退出导致进度丢失
5. **适配uTools**：考虑uTools窗口大小的限制

---

**文档创建日期**：2026-02-09
**最后更新**：2026-02-09
**维护者**：Claude AI Assistant
